[build]
  command = "npm run build:netlify"
  publish = ".output/public"

[build.environment]
  NODE_VERSION = "20"

[functions]
  node_bundler = "esbuild"

# Domain parking is handled by server/middleware/domain-parking.ts
# This middleware checks the hostname and redirects parked domains to /coming-soon
# No redirects needed here - the middleware handles it server-side
