export default defineNitroPlugin((nitroApp) => {
  // This plugin ensures package.json imports are available at runtime
  // The actual fix should be in the build process, but this is a fallback
})


